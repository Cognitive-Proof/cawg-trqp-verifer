// PolicyService is defined in @cognitiveproof/cawg-trqp-plugin-types (the shared contract
// package plugin authors depend on) - re-exported here so existing internal
// imports (`from "./policy_service.js"`) keep working unchanged.
export type { PolicyService } from "@cognitiveproof/cawg-trqp-plugin-types";
